<div class="w-full">
  <h1 class="text-3xl font-bold font-display mb-6">Recently Played</h1>

  <% if @grouped_histories.any? %>
    <div class="space-y-6">
      <% @grouped_histories.each do |album, play_histories| %>
        <div class="bg-gray-800 rounded-lg border border-gray-700 overflow-hidden">
          <%# Album header %>
          <%= link_to album_path(album), class: "flex items-center gap-4 p-4 hover:bg-gray-700/50 transition-colors" do %>
            <%= cover_image_or_placeholder(album.cover_image, size: :tiny, rounded: "rounded") %>
            <div>
              <div class="font-semibold text-white"><%= album.title %></div>
              <div class="text-sm text-gray-400"><%= album.artist.name %></div>
            </div>
          <% end %>

          <%# Tracks under this album %>
          <div class="border-t border-gray-700">
            <% play_histories.each do |ph| %>
              <%= render TrackRow::Component.new(track: ph.track, link_title: true, show_duration: false) do |c| %>
                <% c.with_trailing do %>
                  <span class="text-sm text-gray-400"><%= time_ago_in_words(ph.played_at) %> ago</span>
                <% end %>
              <% end %>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <p class="text-gray-400">No play history yet.</p>
  <% end %>
</div>
